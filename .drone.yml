kind: pipeline
type: docker
name: default

steps:

    - name: test
      image: golang:1.13
      commands:
          - go test ./...
          - go build
      depends_on: [ clone ]

    - name: lint
      image: golangci/golangci-lint
      commands:
          - golangci-lint run
      depends_on: [ clone ]
