package main

import (
	"net/http"
	"os"

	"github.com/ob-algdatii-20ss/leistungsnachweis-teammaze/api"
)

func main() {
	http.Handle("/", api.MazeAPIRouter())
	_ = http.ListenAndServe(os.Getenv("ADDRESS"), nil)
}
